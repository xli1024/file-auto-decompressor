# Example environment file for file-auto-decompressor
# Copy this to .env and modify the values as needed

# Comma-separated list of default passwords to try for password-protected archives
# The system will try these passwords in order when encountering password-protected files
# DEFAULT_PASSWORDS=password123,admin,123456,qwerty,password,12345678,pass,secret

# You can also leave it empty if no default passwords are needed
# DEFAULT_PASSWORDS=
